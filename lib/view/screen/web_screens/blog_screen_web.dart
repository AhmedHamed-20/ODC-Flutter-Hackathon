import 'package:flutter/material.dart';

class BlogScreenWeb extends StatelessWidget {
  const BlogScreenWeb({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

import 'package:flutter/material.dart';

class CommunityScreenWeb extends StatelessWidget {
  const CommunityScreenWeb({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
